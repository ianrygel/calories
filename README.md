# LeanPath

A static Next.js body fat and calorie calculator.

## Local Development

```bash
npm install
npm run dev
```

## GitHub Pages

The workflow in `.github/workflows/pages.yml` builds the app as a static export and deploys `out/` to GitHub Pages.

Repository Pages URL:

```text
https://ianrygel.github.io/calories/
```
